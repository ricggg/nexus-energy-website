export const metadata = { title: "Compliance | Nexus Energy" };

export default function CompliancePage() {
  return (
    <div className="container-page section-pad max-w-3xl">
      <h1 className="font-display text-3xl font-bold text-[var(--color-text)]">Compliance</h1>
      <p className="mt-6 text-[15px] leading-relaxed text-[var(--color-text-muted)]">
        This placeholder compliance page describes, in a production deployment, Nexus Energy's
        ethics, regulatory, and anti-corruption compliance program, along with a reporting channel
        for concerns. Replace this section with counsel-reviewed content before launch.
      </p>
    </div>
  );
}
